flask==3.0.0
flask-cors==4.0.0
# loosen langchain pins so pip can resolve a compatible langchain-core
langchain-openai
langgraph
langchain-mcp-adapters
python-dotenv==1.0.0
aiohttp==3.9.0
# `asyncio` is part of the Python stdlib for modern Python; remove strict pip package