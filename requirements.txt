Flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
gunicorn==20.1.0

# Optional / advanced dependencies (LLM, MCP) moved to `requirements-llm.txt`.
# If you enable full MCP/LLM features on Render, install those as well.