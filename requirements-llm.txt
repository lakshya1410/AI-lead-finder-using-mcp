# Optional/advanced dependencies for MCP and LLM features.
# Install these only if you plan to enable the agent functionality.

langchain-openai
langgraph
langchain-mcp-adapters
aiohttp==3.9.0
# If you need a specific langchain/core pin, add it here.
# Example: langchain==0.0.x

# Note: some of these packages may require additional system libraries
# or specific Python versions. Install locally and pin working versions
# before deploying to Render.
